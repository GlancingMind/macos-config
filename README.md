# Setup

1. Install nix via determinant nix installer
2. Invoke: `nix run --extra-experimental-features nix-command --extra-experimental-features flakes  "nix-darwin/nix-darwin-24.11#darwin-rebuild" -- switch --flake /Users/sascha/workspace/os-config`

